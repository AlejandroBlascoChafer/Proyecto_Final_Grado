package com.example.proyecto_final_grado.listeners

import com.example.proyecto_final_grado.models.EditListEntryItem

interface OnEditListClickListener {
    fun onEditListListener(entry: EditListEntryItem)
}