query GetUserOptionsQuery {
    Viewer {
        options {
            titleLanguage
            displayAdultContent
            staffNameLanguage
            airingNotifications
        }
        mediaListOptions {
            scoreFormat
        }
    }
}
