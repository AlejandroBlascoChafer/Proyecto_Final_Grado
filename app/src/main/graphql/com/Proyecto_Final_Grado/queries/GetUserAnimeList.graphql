query GetUserAnimeList($userName: String!, $status: MediaListStatus) {
  MediaListCollection(userName: $userName, status: $status, type: ANIME, sort: SCORE_DESC) {
    lists {
      entries {
        status
        score
        progress
        media {
          title {
            romaji
          }
          coverImage {
            medium
          }
        }
      }
    }
  }
}
